\documentclass{standalone}
\usepackage{tikz, ifthen}
\usepackage{amsmath}

\begin{document}

\begin{tikzpicture}
    
    % Center of the circle
    \def\xmin{-2}
    \def\xmax{7}
    \def\ymin{-2}
    \def\ymax{7}
    \def\dx{.5}
    \def\dy{.5}
    \def\xminb{\xmin-\dx}
    \def\xmaxb{\xmax+\dx}
    \def\yminb{\ymin-\dy}
    \def\ymaxb{\ymax+\dy}
    \def\xa{1}     % xA
    \def\ya{3}     % yA
    \def\xb{5}     % xB
    \def\yb{6}     % yB
    \def\xpmin{-6}
    \def\xpmax{ 6}
    \def\ypmin{-5.5}
    \def\ypmax{ 5.5}
%   \def\a{0.5};
%   \coordinate (fa) at (\xa, \ya);
%   \coordinate (fb) at (\xb, \yb);
%   \def\c{sqrt((\xa-\xb)^2+(\ya-\yb)^2)/2};
%   \def\xc{0.5*(\xa+\xb)};
%   \def\yc{0.5*(\ya+\yb)};
%   \coordinate (center) at ({\xc}, {\yc});
%   \def\b{sqrt(\c^2-\a^2)};
%   \def\alpha{{atan2(\yb-\ya,\xb-\xa)}};

    \clip (\xminb, \yminb) rectangle (\xmaxb, \ymaxb);

    % Draw grid
    \draw[step=1., gray!60, ultra thin] (\xmin-2, \ymin-2) grid (\xmax+2, \ymax+2); % Visible grid with lighter color
    % Axes, x
    \draw[->, thick, black] (\xmin, 0) -- (\xmax, 0) node[below right] {$x$};
    \draw[->, thick, black] (0, \ymin) -- (0, \ymax) node[right] {$y$};
    \draw[    thick, black] (\xmin-2, 0) -- (\xmax+2, 0);
    \draw[    thick, black] (0, \ymin-2) -- (0, \ymax+2);
    \draw[->, thick, black] (0,0) -- (\xa,\ya);
    \draw[->, thick, black] (0,0) -- (\xb,\yb);
    \draw[->, thick, black] (\xa,\ya) -- (\xb,\yb) node [midway,above=5,left=-3] {$\vec{AB}$};
    \draw[->, thick, black] (0,0) -- (\xb,\yb) node [midway,above=5,left=-3] {$\vec{OB}$};
    \draw[->, thick, black] (0,0) -- (\xa,\ya) node [midway,above=20,left=-8] {$\vec{OA}$};
    % Axis ticks
    \foreach \x in {\xmin,..., \xmax}{
        \ifthenelse{ \x = 0 }{}{\draw[black] (\x, -0.1) -- (\x, +0.1) node[below=8pt, left=0pt] {\small $\x$};}
    }
    \foreach \y in {\ymin,..., \ymax}{
        \ifthenelse{ \y = 0 }{}{\draw[black] (-0.1, \y) -- (0.1, \y) node[left=4pt] {\small $\y$};}
    }
    % Origin
    \filldraw[black] (0,0) circle (1pt) node[below left] {$O$};

    \filldraw[black] (\xa,\ya) circle (1pt) node[above left] {$A$};
    \filldraw[black] (\xb,\yb) circle (1pt) node[above left] {$B$};


%   % Foci
%   \filldraw[black] (\xa, \ya) circle (2pt) node[above right] {$0$};
%   \filldraw[black] (\xb, \yb) circle (2pt) node[above right] {$2$};
    
\end{tikzpicture}

\end{document}
